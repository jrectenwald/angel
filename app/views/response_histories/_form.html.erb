<%= form_for(response_history) do |f| %>
  <% if response_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response_history.errors.count, "error") %> prohibited this response_history from being saved:</h2>

      <ul>
      <% response_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
